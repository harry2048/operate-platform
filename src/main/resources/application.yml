server:
  port: 8010
spring:
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost:3306/operate_platform2?serverTimezone=GMT%2B8&allowMultiQueries=true&characterEncoding=UTF-8
    username: root
    password: root
  application:
    name: matadata
logging:
  level:
    com:
      zr: debug
mybatis:
  mapper-locations: classpath:mybatis-mapper/*Mapper.xml